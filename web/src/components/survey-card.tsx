/*
  survey-card.tsx — One card in the dashboard survey list.
  Shows: title, slug, response count, and action buttons (Edit, Share, View Responses).
  Reused in the empty state and the full grid.
*/
