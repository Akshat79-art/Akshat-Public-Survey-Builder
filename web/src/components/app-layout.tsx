/*
  app-layout.tsx — The authenticated shell wrapped around every dashboard route.
  Adds: nav bar, user menu, sign-out. Keeps <Outlet /> so child routes
  render their content inside this shell.
*/
