/*
  question-fields.tsx — Renders the right form field for each question_type.
  Shared between the builder (preview/edit) and the public survey (answer).
  Keeps the switch statement in one place instead of repeating it per route.
*/
